<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>About — The Most Gorgeous Website Ever</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;0,900;1,400;1,700;1,900&family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400;1,600&family=Outfit:wght@200;300;400&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --ivory: #F5F0E8;
      --warm-black: #1A1714;
      --gold: #C9A84C;
      --gold-light: #E4D5A0;
      --blush: #D4A69A;
      --deep-wine: #5C2434;
      --sage: #8B9A77;
    }

    body {
      background: var(--warm-black);
      color: var(--ivory);
      font-family: 'Cormorant Garamond', serif;
      overflow-x: hidden;
    }

    body::before {
      content: '';
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
      pointer-events: none;
      z-index: 9999;
    }

    /* === NAV === */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      padding: 2rem 3rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      z-index: 100;
      mix-blend-mode: difference;
    }

    nav a {
      font-family: 'Outfit', sans-serif;
      font-weight: 200;
      font-size: 0.75rem;
      letter-spacing: 0.4em;
      text-transform: uppercase;
      color: var(--ivory);
      text-decoration: none;
      transition: color 0.3s ease;
    }

    nav a:hover {
      color: var(--gold);
    }

    .nav-links {
      display: flex;
      gap: 3rem;
    }

    /* === HERO === */
    .about-hero {
      min-height: 70vh;
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      padding: 4rem 3rem;
      position: relative;
    }

    .about-hero::before {
      content: '';
      position: absolute;
      width: 50vw;
      height: 50vw;
      max-width: 600px;
      max-height: 600px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(201,168,76,0.06) 0%, transparent 70%);
      top: 20%; right: -10%;
      pointer-events: none;
    }

    .about-label {
      font-family: 'Outfit', sans-serif;
      font-weight: 200;
      font-size: 0.7rem;
      letter-spacing: 0.5em;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 2rem;
      opacity: 0;
      animation: fadeUp 1s ease 0.2s forwards;
      display: flex;
      align-items: center;
      gap: 1.5rem;
    }

    .about-label::before {
      content: '';
      width: 40px;
      height: 1px;
      background: var(--gold);
      opacity: 0.5;
    }

    .about-title {
      font-family: 'Playfair Display', serif;
      font-weight: 900;
      font-style: italic;
      font-size: clamp(3rem, 8vw, 7rem);
      line-height: 1;
      letter-spacing: -0.02em;
      background: linear-gradient(135deg, var(--ivory) 0%, var(--gold-light) 50%, var(--blush) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      max-width: 700px;
      overflow: hidden;
    }

    .about-title span {
      display: inline-block;
      transform: translateY(110%);
      animation: revealUp 1s cubic-bezier(0.22, 1, 0.36, 1) 0.4s forwards;
    }

    @keyframes revealUp {
      to { transform: translateY(0); }
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* === CONTENT === */
    .about-content {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1px;
      background: rgba(201,168,76,0.06);
      margin-top: 1px;
    }

    @media (max-width: 768px) {
      .about-content { grid-template-columns: 1fr; }
    }

    .about-block {
      background: var(--warm-black);
      padding: clamp(3rem, 5vw, 6rem);
      position: relative;
    }

    .about-block.full-width {
      grid-column: 1 / -1;
    }

    .block-number {
      font-family: 'Playfair Display', serif;
      font-size: 6rem;
      font-weight: 900;
      color: transparent;
      -webkit-text-stroke: 1px rgba(201,168,76,0.08);
      position: absolute;
      top: 1rem;
      right: 2rem;
      line-height: 1;
      pointer-events: none;
    }

    .block-heading {
      font-family: 'Outfit', sans-serif;
      font-weight: 200;
      font-size: 0.7rem;
      letter-spacing: 0.4em;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 2rem;
    }

    .block-text {
      font-family: 'Cormorant Garamond', serif;
      font-weight: 300;
      font-size: clamp(1.1rem, 1.8vw, 1.4rem);
      line-height: 1.9;
      color: rgba(245,240,232,0.7);
      max-width: 500px;
    }

    .block-text em {
      color: var(--gold-light);
      font-style: italic;
    }

    /* === VALUES === */
    .values-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1px;
      background: rgba(201,168,76,0.06);
    }

    @media (max-width: 768px) {
      .values-grid { grid-template-columns: 1fr; }
    }

    .value-card {
      background: var(--warm-black);
      padding: clamp(2.5rem, 4vw, 4rem);
      text-align: center;
      transition: background 0.5s ease;
    }

    .value-card:hover {
      background: #1f1c17;
    }

    .value-icon {
      font-size: 2rem;
      margin-bottom: 1.5rem;
      display: block;
      opacity: 0.6;
    }

    .value-title {
      font-family: 'Playfair Display', serif;
      font-weight: 700;
      font-size: 1.3rem;
      color: var(--ivory);
      margin-bottom: 1rem;
    }

    .value-desc {
      font-family: 'Cormorant Garamond', serif;
      font-weight: 300;
      font-size: 1rem;
      line-height: 1.8;
      color: rgba(245,240,232,0.45);
    }

    /* === CTA === */
    .cta-section {
      min-height: 50vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 6rem 2rem;
      position: relative;
    }

    .cta-section::before {
      content: '';
      position: absolute;
      width: 300px; height: 300px;
      border: 1px solid rgba(201,168,76,0.05);
      border-radius: 50%;
      pointer-events: none;
      animation: pulse 4s ease-in-out infinite;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); opacity: 0.3; }
      50% { transform: scale(1.15); opacity: 0.6; }
    }

    .cta-text {
      font-family: 'Playfair Display', serif;
      font-weight: 400;
      font-style: italic;
      font-size: clamp(1.5rem, 3vw, 2.5rem);
      color: var(--ivory);
      margin-bottom: 3rem;
      max-width: 500px;
    }

    .cta-link {
      font-family: 'Outfit', sans-serif;
      font-weight: 300;
      font-size: 0.8rem;
      letter-spacing: 0.4em;
      text-transform: uppercase;
      color: var(--gold);
      text-decoration: none;
      padding: 1.2rem 3rem;
      border: 1px solid rgba(201,168,76,0.3);
      transition: all 0.4s ease;
    }

    .cta-link:hover {
      background: rgba(201,168,76,0.08);
      border-color: var(--gold);
    }

    /* === FOOTER === */
    .footer {
      padding: 4rem 2rem;
      text-align: center;
      border-top: 1px solid rgba(201,168,76,0.06);
    }

    .footer-diamond {
      width: 6px; height: 6px;
      background: var(--gold);
      transform: rotate(45deg);
      margin: 0 auto 1.5rem;
      opacity: 0.3;
    }

    .footer-text {
      font-family: 'Outfit', sans-serif;
      font-weight: 200;
      font-size: 0.7rem;
      letter-spacing: 0.5em;
      text-transform: uppercase;
      color: rgba(245,240,232,0.2);
    }

    /* Scroll reveal */
    .reveal {
      opacity: 0;
      transform: translateY(30px);
      transition: all 0.8s cubic-bezier(0.22, 1, 0.36, 1);
    }

    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }
  </style>
</head>
<body>

  <nav>
    <a href="index.html">Gorgeous</a>
    <div class="nav-links">
      <a href="index.html">Home</a>
      <a href="about.html">About</a>
    </div>
  </nav>

  <!-- HERO -->
  <section class="about-hero">
    <div class="about-label">About</div>
    <h1 class="about-title"><span>The Story Behind the Beauty</span></h1>
  </section>

  <!-- CONTENT GRID -->
  <div class="about-content">
    <div class="about-block reveal">
      <div class="block-number">01</div>
      <div class="block-heading">Origin</div>
      <p class="block-text">
        This website was born from a single, <em>audacious</em> premise — that a webpage could make you feel something. Not sell you something. Not distract you. Just stop you in your tracks.
      </p>
    </div>

    <div class="about-block reveal">
      <div class="block-number">02</div>
      <div class="block-heading">Philosophy</div>
      <p class="block-text">
        Every pixel is <em>deliberate</em>. Every animation, earned. We believe restraint is louder than noise, and that true elegance lives in the space between elements.
      </p>
    </div>

    <div class="about-block full-width reveal">
      <div class="block-number">03</div>
      <div class="block-heading">Craft</div>
      <p class="block-text">
        Built with nothing but HTML, CSS, and an <em>unreasonable</em> attention to detail. No frameworks. No libraries. Just code and conviction. This is proof that gorgeous doesn't require complexity — it requires care.
      </p>
    </div>
  </div>

  <!-- VALUES -->
  <div class="values-grid">
    <div class="value-card reveal">
      <span class="value-icon">◆</span>
      <div class="value-title">Precision</div>
      <p class="value-desc">Every detail measured, every choice intentional</p>
    </div>
    <div class="value-card reveal">
      <span class="value-icon">◈</span>
      <div class="value-title">Atmosphere</div>
      <p class="value-desc">Design that creates a feeling, not just a layout</p>
    </div>
    <div class="value-card reveal">
      <span class="value-icon">◇</span>
      <div class="value-title">Restraint</div>
      <p class="value-desc">Knowing what to leave out is the hardest skill</p>
    </div>
  </div>

  <!-- CTA -->
  <section class="cta-section reveal">
    <p class="cta-text">Ready to return to the gorgeousness?</p>
    <a href="index.html" class="cta-link">Back to Home</a>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="footer-diamond"></div>
    <p class="footer-text">The Most Gorgeous Website Ever</p>
  </footer>

  <script>
    const reveals = document.querySelectorAll('.reveal');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) entry.target.classList.add('visible');
      });
    }, { threshold: 0.15 });
    reveals.forEach(el => observer.observe(el));
  </script>
</body>
</html>
